# Optional. Local development defaults to http://localhost:5000 and production
# defaults to same-origin /api requests through the Firebase Hosting rewrite.
# Set this only when the API is hosted on a separate origin.
# VITE_API_URL=https://api.example.com
VITE_GOOGLE_MAPS_API_KEY=your_http_referrer_restricted_google_maps_key
